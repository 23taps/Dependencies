# ASN1Swift 1.2.3 — XCFramework build record

Generated by `Tools/build-xcframework.sh ASN1Swift`. Do not edit by hand.

| | |
| --- | --- |
| Upstream | https://github.com/tikhop/ASN1Swift.git |
| Source tag | `1.2.3` |
| Resolved commit | `0f3150de37d38190768caaff19c498d85efad715` |
| Distribution keys | `1.2.3` `1.2.3-no-bitcode` |
| Manifest | `ASN1Swift/ASN1Swift-xcframework.json` |
| Toolchain | Xcode 27.0 (Build version 27A266a) |
| Swift | Apple Swift version 6.4 (swiftlang-6.4.0.34.1 clang-2100.3.34.1) |
| Deployment target | iOS 15.0 |
| Bundle identifier | `com.emoji.ASN1Swift` |
| Built | 2026-09-14 19:33:15 UTC |
| Archive SHA-256 | `1df23c3dfd6b3ebf6f089377e3186073a57c5e989c26c246d1ef55fdb0adb21c` |

Slices:

| Library identifier | Architectures | Mach-O platform |
| --- | --- | --- |
| `ios-arm64` | arm64 | 2 (iOS) |
| `ios-arm64_x86_64-simulator` | x86_64, arm64 | 7 (iOS Simulator) |

Device `armv7` and simulator `i386` are intentionally absent: neither is
supported by current Xcode, and the app's iOS 15 minimum does not need them.

Packaging changes applied to the pinned source (no implementation source was
modified):

- `Package.swift` library product forced to `type: .dynamic`, so
  `xcodebuild archive` installs a framework and the app keeps embedding
  ASN1Swift as a dynamic framework.
- `IPHONEOS_DEPLOYMENT_TARGET=15.0`; upstream declares iOS 10, which current
  Xcode no longer supports, and the app requires iOS 15.
- `PrivacyInfo.xcprivacy` copied in to match the previously distributed binary.

Note for consumers: 1.2.3 returns `Data` built with
`Data(bytesNoCopy:deallocator:.none)` pointing into the caller's buffer, so a
decoded `Data` is only valid while the source `Data` is alive. That is upstream
behaviour, unchanged by this rebuild.

